/* var com = 1
var fim = 10
while (com <= fim){
    console.log(com)
    com ++
} */

var tab = 2
var mult = -0
var resl = 0

while (mult <= 9){
    resl = (tab * mult)
    mult++
    console.log(`${tab} x ${mult} = ${resl}`)
}

